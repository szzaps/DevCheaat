# ============================================================
# PYTHON — DEVELOPMENT & TOOLS
# ============================================================

# ------------------------------------------------------------
# Core language and idioms
# ------------------------------------------------------------
# Python 3.8+: f-strings, walrus operator :=, positional-only parameters
# Functions, classes, context managers, comprehensions
# Type hints: Optional, Union, List, Dict, Any, TypedDict, Protocol
# Dataclasses: from dataclasses import dataclass
# Decorators, closures, descriptors, contextlib.contextmanager

# ------------------------------------------------------------
# Standard library highlights
# ------------------------------------------------------------
import os
import sys
import pathlib
import functools
import itertools
import collections
import threading
import multiprocessing
import asyncio
import logging
import json
import csv
import struct
import mmap
import socket
import ssl
import subprocess
import time
import datetime
import argparse

# ------------------------------------------------------------
# Popular libraries
# ------------------------------------------------------------
# Web/API: requests, httpx, aiohttp, Flask, FastAPI, Django
# Data: numpy, pandas, scipy, scikit-learn
# Parsing: lxml, beautifulsoup4
# Binary: pyelftools, capstone, construct
# CLI tools: click, typer
# Async: trio, anyio
# Packaging: setuptools, wheel, twine, poetry

# ------------------------------------------------------------
# Tooling & workflow
# ------------------------------------------------------------
# Virtual environments: python -m venv venv
# Dependency management: pip install -r requirements.txt
# Formatting/linting: black, isort, flake8, ruff, mypy
# Testing: pytest, hypothesis
# CI/CD: GitHub Actions, GitLab CI, tox
# Containerization: Dockerfile, multi-stage builds

# ------------------------------------------------------------
# Debugging & profiling
# ------------------------------------------------------------
# Debuggers: pdb, ipdb, pudb, debugpy
# Profilers: cProfile, pyinstrument, py-spy, scalene
# Memory analysis: tracemalloc, objgraph
# Line profiling: line_profiler

# ------------------------------------------------------------
# Concurrency and parallelism
# ------------------------------------------------------------
# threading for I/O-bound tasks
# multiprocessing for CPU-bound tasks
# asyncio for async tasks
# concurrent.futures for thread/process pools